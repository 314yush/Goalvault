/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add other Next.js configurations here if needed in the future
  // For example, experimental features, redirects, rewrites, etc.
};

export default nextConfig; 